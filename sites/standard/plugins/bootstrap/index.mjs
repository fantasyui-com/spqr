export default async function main(context){

  // called during build process, you can add/merge files
  // for example handlebars-template(site-root, './robots-txt')
  // or handlebars-template(site-root, './bootstrap/4.0-stack/')

}
